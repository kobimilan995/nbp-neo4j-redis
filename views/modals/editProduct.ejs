<div class="modal fade" id="edit-product-modal-<%= product.id %>">
  <div class="modal-dialog" role="document">
    <form method="POST" action="/product/update">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title">Edit product: <%= product.title %></h5>
        </div>
        <div class="modal-body">
            <div class="form-group">
              <label for="productTitle">Title</label>
              <input type="text" name="title" class="form-control" id="productTitle" value="<%= product.title%>" placeholder="Enter title">
            </div>
            <div class="form-group">
              <label for="productDescription">Description</label>
              <input type="text" name="description" class="form-control" value="<%= product.description %>" id="productDescription" placeholder="Description">
            </div>

            <div class="form-group">
              <label for="productPrice">Price</label>
              <input type="number" name="price" class="form-control" value="<%= product.price %>" id="productPrice" placeholder="Price">
            </div>
            <input type="hidden" name="id" value="<%= product.id%>">
        </div>
        <div class="modal-footer">
          <button type="submit" class="btn btn-primary">Edit</button>
          <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
        </div>
      </div>
    </form>
  </div>
</div>